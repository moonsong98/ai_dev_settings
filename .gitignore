# OS
.DS_Store
Thumbs.db

# Neovim
nvim/plugin/

# Sensitive data
claude/credentials*
claude/.oauth*
*.key
*.pem

# Local overrides (per-machine config, never committed)
nvim/lua/config/local.lua
tmux/tmux.local.conf
